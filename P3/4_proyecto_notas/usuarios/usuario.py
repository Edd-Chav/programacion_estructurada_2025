import conexionBD 
import datetime

def registrar(nombre,apellidos,email,contrasena):
    try:
        fecha=datetime.datetime.now()
        sql=""
        val=()
        cursor.execute()
        return True
    except:
        return False
